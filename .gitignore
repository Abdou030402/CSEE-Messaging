# Secrets / local config (never commit) — committed *.example counterparts exist
.env
token.json

# Local data with real message examples / real signature (PII)
# NOTE: recipients_email.txt / recipients_slack.txt are committed as example
# test data for now; they'll be replaced by a database lookup later.
examples/historical_messages.md
signature.txt

# Build / editor cruft
__pycache__/
*.pyc
.claude/settings.local.json

repo.txt
