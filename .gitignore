# Secrets / local config (never commit) — committed *.example counterparts exist
.env
token.json

# Local data with real recipients / real message examples / real signature (PII)
recipients_email.txt
recipients_slack.txt
examples/historical_messages.md
signature.txt

# Build / editor cruft
__pycache__/
*.pyc
.claude/settings.local.json

repo.txt
